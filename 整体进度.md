# Tianci OA 整体进度

## 项目信息

- 前端：`Tianci-OA-Web`
- 后端：`Tianci-OA-Api`
- 当前进度：简历附件、功能权限和数据权限已完成一轮改造，已通过构建与单元测试。
- 最后更新：2026-08-06

## 已完成

### 简历附件

- 调整为“先保存简历，再上传附件”的两阶段流程。
- 简历详情支持上传、进度显示、附件列表、下载、设为当前简历和删除。
- 文件上传后自动绑定简历业务记录，不再要求用户手工填写文件 ID。
- 后端增加简历主附件关联接口，并校验文件所属业务、状态和乐观锁版本。

### 功能权限

- 初始化脚本已补齐 27 个菜单及操作权限标识，覆盖组织、员工、招聘、合同、文件、审计、工作流和系统管理。
- 预置超级管理员、HR 管理员、部门管理员、面试官和普通员工的权限组合。
- 组织查看和组织管理已拆分，上传、下载、删除文件也按操作独立授权。
- “安排面试”和“提交面试评价”已拆分为独立权限，面试官默认只能评价分配给本人的面试。
- 修改角色或菜单授权后，会失效受影响用户的权限缓存和旧会话，防止旧权限继续生效。
- 前端仪表盘、路由、按钮和文件操作已按对应权限显示，后端仍作为最终强制校验。

### 数据权限

- 角色数据范围支持：全部数据、本部门及所有下级部门、仅本人。
- 用户具有多个角色时取最宽数据范围：全部 > 部门及下级 > 仅本人。
- 员工的“仅本人”以 `sys_user.employee_id` 绑定的员工为准。
- 合同、入职记录的“仅本人”通过绑定员工 ID 关联判定。
- 简历的“仅本人”按简历负责人 `owner_user_id` 或面试任务的 `interviewer_user_id` 判定。
- 部门范围从用户所属部门出发，递归包含所有下级部门。
- 文件权限继承其关联的简历、员工、入职或合同记录的数据范围。

### 文档和测试

- 后端 API 文档已补充简历附件流程、功能权限和数据权限说明。
- 后端 Release 测试：38/38 通过。
- 前端 `vue-tsc --noEmit` 通过。
- 前端 Vite 生产构建通过。

## 部署注意

- 已有数据库需重新执行 `Tianci-OA-Api/database/init.sql`，才会获得新角色、菜单权限及授权关系。
- 账号使用“仅本人”数据范围前，应在用户管理中绑定员工；未绑定时，员工、合同和入职记录默认不可见。
- 功能权限决定“能否调用某个功能”，数据权限决定“在该功能中能看到和操作哪些记录”，两者需同时满足。

## 待后续验收

- 在真实 MySQL 实例中执行初始化脚本，分别用 HR、部门管理员、面试官和普通员工账号完成权限矩阵验收。
- 根据实际部署体积决定是否对前端大体积 chunk 继续做依赖拆包。
